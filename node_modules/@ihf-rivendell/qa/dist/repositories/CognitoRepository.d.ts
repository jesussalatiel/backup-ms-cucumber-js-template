import { CognitoIdentityProviderClient } from "@aws-sdk/client-cognito-identity-provider";
import { BaseRepository, BaseRepositoryProps } from "./BaseRepository";
export interface CognitoRepositoryProps extends BaseRepositoryProps {
    cognitoClient: CognitoIdentityProviderClient;
    userPoolId: string;
}
export declare class CognitoRepository extends BaseRepository {
    protected props: CognitoRepositoryProps;
    constructor(props: CognitoRepositoryProps);
    changePassword(username: string, password: string): Promise<void>;
    initiateAuth(number: string, password: string): Promise<{
        token: string;
    }>;
}
