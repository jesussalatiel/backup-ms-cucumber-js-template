import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import { BaseRepository, BaseRepositoryProps } from "./BaseRepository";
export interface MambuRepositoryProps extends BaseRepositoryProps {
    dynamoDbClient: DynamoDBDocumentClient;
    mambuLoansTable: string;
    mambuCustomersTable: string;
}
export declare class MambuRepository extends BaseRepository {
    protected props: MambuRepositoryProps;
    constructor(props: MambuRepositoryProps);
    findLoansFromMambu(loanId: string): Promise<Record<string, any>>;
    updateInstallmentsStatus(loanId: string, status: string, installmentIds: string[]): Promise<void>;
    findCustomersFromMambu(customerId: string): Promise<Record<string, any>>;
}
