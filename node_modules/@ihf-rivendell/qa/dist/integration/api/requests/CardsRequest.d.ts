import { BaseRequests } from "./BaseRequest";
import { IHttpBuilder } from "../../assets/interface/IHttpBuilder";
export declare class CardsRequests extends BaseRequests {
    protected props: IHttpBuilder;
    private cardsRepository;
    private table;
    private customerId;
    private sourceIp;
    private cardId;
    constructor(props: IHttpBuilder, shouldPrintRequest?: boolean);
    createCard(): Promise<any>;
    generateBines(transactionToken: any): Promise<any>;
    getCard(): Promise<any>;
    makePayment(): Promise<any>;
    delete(cardId: string): Promise<any>;
    private buildGenerateBines;
    private buildCreatePaymentPromises;
}
