import { BaseRequests } from "./BaseRequest";
import { IHttpBuilder } from "../../assets/interface/IHttpBuilder";
export declare class ContractsRequests extends BaseRequests {
    protected props: IHttpBuilder;
    private contractsRepository;
    private customerId?;
    private loanId?;
    constructor(props: IHttpBuilder, shouldPrintRequest?: boolean);
    createContract(): Promise<any>;
    contractSignature(contractId: string): Promise<any>;
    findContract(contractId: string): Promise<any>;
    private buildContractSignature;
    private buildCreateContractPromise;
}
