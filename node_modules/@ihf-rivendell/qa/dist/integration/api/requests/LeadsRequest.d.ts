import { BaseRequests } from "./BaseRequest";
import { IHttpBuilder } from "../../assets/interface/IHttpBuilder";
export declare class LeadsRequests extends BaseRequests {
    protected props: IHttpBuilder;
    private table?;
    private customerId?;
    private leadsRepository;
    private customerResponse?;
    constructor(props: IHttpBuilder, shouldPrintRequest?: boolean);
    create(): Promise<any>;
    findByIdentityDocument(props: {
        documentType: string;
        documentNumber: string;
        amount?: number;
        currency?: string;
        status?: "ACTIVE";
    }): Promise<any>;
    deleteAllLeads(): Promise<any>;
    find(leadId: string): Promise<any>;
    createSale(leadId: string): Promise<any>;
    findLeadByStatus(status: string): Promise<any>;
    findExternalLead(props: {
        documentType: string;
        documentNumber: string;
        email?: string;
        mobile?: string;
    }): Promise<any>;
    private buildFindLeadByStatus;
    private buildSale;
    private buildDeleteAllLeads;
    private buildFindByIdentityDocument;
    private buildCreateLead;
}
